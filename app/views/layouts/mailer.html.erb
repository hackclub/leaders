<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <style>
      /* Email styles need to be inline */
      * {
        box-sizing: border-box;
        font-weight: inherit;
        -webkit-appearance: none;
        -moz-appearance: none;
      }
      html,
      body {
        min-height: 100%;
        min-width: 100%;
      }
      body {
        padding: 0;
        margin: 0;
        font-size: 16px;
        font-family: -apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,sans-serif;
        line-height: 1.375;
        color: #384046;
      }
      h1 {
        color: #e42d42;
      }
      a {
        color: #2d9ee4;
      }
      h1,
      h2,
      h3,
      a,
      strong {
        font-weight: bold;
      }
      .btn {
        display: inline-block;
        text-align: center;
        text-decoration: none;
        font-size: 24px;
        font-weight: bold;
        line-height: 1.125;
        appearance: none;
        cursor: pointer;
        margin: .5rem 0;
        box-shadow: 0 2px 6px rgba(0,0,0,.125), 0 4px 12px rgba(0,0,0,.0625);
        border-radius: 9999px;
        border: none;
        background: #e42d42 !important;
        color: #fff !important;
        padding: 12px 24px;
      }
    </style>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
